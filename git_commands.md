```
git init

git add .

git commit -m 'lecture42'


to push files to bitbucket run:
git remote add origin https://shbeta@bitbucket.org/xxxx/webschool.git
git push -u origin master

to get repo from bitbucket run:
https://bitbucket.org/xxxx/webschool/get/5d76bba.tar.gz

```
